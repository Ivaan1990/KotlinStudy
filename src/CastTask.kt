fun main(args: Array<String>){
    var five: Char = '5'

    var fiveStr = five.toString()

    var byteStr = fiveStr.toByte()
    var shortStr = fiveStr.toShort()
    var intStr = fiveStr.toInt()
    var doubleStr = fiveStr.toDouble()
    var floatStr = fiveStr.toFloat()
    var longStr = fiveStr.toLong()
}